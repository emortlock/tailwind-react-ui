import React from 'react'

import Button from './Button'

/**
 * @see See [Button](#button-2) for API. Sets `buttonStyle` prop to `fill`.
 */
const FillButton = props => <Button {...props} buttonStyle="fill" />

export default FillButton
