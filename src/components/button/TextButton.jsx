import React from 'react'

import Button from './Button'

/**
 * @see See [Button](#button-2) for API. Sets `buttonStyle` prop to `text`.
 */
const TextButton = props => <Button {...props} buttonStyle="text" />

export default TextButton
